# MediOrbit backend — pinned production dependencies
# Ollama/MedGemma handles all AI inference locally. No cloud SDK required.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# Async Ollama client + file streaming
httpx==0.27.2
aiofiles==24.1.0

# LangGraph agent orchestration
langgraph==0.2.28
langchain-core==0.2.38

# Data validation
pydantic==2.8.2

# OCR / prescription parsing
Pillow==10.4.0
pytesseract==0.3.13
pdfplumber==0.10.3

# Biomedical NER (optional — loaded at startup, gracefully skipped if unavailable)
torch==2.4.1
transformers==4.44.2

# Rate limiting
slowapi==0.1.9

# Utilities
icd9cms
