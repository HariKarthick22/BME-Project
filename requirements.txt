# MediOrbit — Python dependencies
# All AI inference runs locally via Ollama/MedGemma. No cloud API keys required.

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# --- LLM orchestration (local) ---
# Ollama is the runtime — install separately: https://ollama.ai
httpx>=0.27.0          # async Ollama client
aiofiles>=23.0.0       # stream large file uploads

# --- Agent orchestration ---
langgraph>=0.2.0
langchain-core>=0.2.0

# --- Data validation ---
pydantic>=2.6.0

# --- OCR / prescription parsing ---
pytesseract>=0.3.10
Pillow>=10.2.0
pdfplumber>=0.10.0

# --- Biomedical NER (optional, loads on startup if available) ---
torch
transformers

# --- Utilities ---
icd9cms
